#ifndef imageprocess_hpp
#define imageprocess_hpp

#include <stdio.h>
#include <SFML/Graphics.hpp>

class ImageProcess {
public:
    static sf::Image toReferenceColors(sf::Image image);
//    static sf::Image convert(std::map<>)
};

#endif /* imageprocess_hpp */
